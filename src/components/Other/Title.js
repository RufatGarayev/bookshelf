const Title = ({ title, description }) => {
    return (
        <div className="section-title text-center">
            <h5>{title}</h5>
            <p>{description}</p>
        </div>
    )
}


export default Title;